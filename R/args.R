#' @title
#' Package Arguments
#'
#' @param version       OncoTree version. One of "oncotree_latest_stable," "oncotree_candidate_release," or "oncotree_development".
#' @param type one of "code", "name", "mainType", "level", "nci", "umls", or "color"
#' @param search character vector of length one of the query
#' @param exact_match If it sets to true, it will only return one element array.
#' @param levels vector of integers ranging from 1:5. Level 1 is equivalent to the Tissue Level.
#'
#' @name pkg_args
NULL
